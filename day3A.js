function multiplicationTable(number){

    
    for(let i=1; i<=10; i++){
        
        let table = number*i;
        console.log(`${number} x ${i} = ${table}`);
    }
}
multiplicationTable(7);
